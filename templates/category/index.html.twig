{% extends 'base.html.twig' %}

{% block title %}Hello ! Welcome in Categories Controller !{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <div><a class="btn btn-primary pull-left" href="/ajout_categorie">Ajout d'une catégorie</a></p></div>
    <div class="help-block" style="height:50px;"></div>
    {% for c in categorie %}
        <div class="jumbotron">
            <p>{{c.name}}</p>
            <p>{{c.slug}}</p>
            <a href="/supprimer_categorie/{{c.id}}" class="btn btn-danger pull-right">Supprimer</a></td>
            <p>Evénements liés:</p>
            {% for news in c.news %}
                <ul>
                    <li>{{ news.title }}</li>
                </ul>
            {% endfor %}

        </div>
    {% endfor%}
{% endblock %}

